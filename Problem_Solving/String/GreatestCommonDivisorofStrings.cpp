// not solved Yet

#include <vector>
#include <string>
#include <unordered_map>
#include <iostream>

using namespace std;

string gcdOfStrings(string str1, string str2) {
    if(str1.size() %str2.size() !=0) return "";
}