#include <string>
#include <iostream>

using namespace std ;
double myPow(double x, int n) {
    string s = to_string(x);
    string num = "" ;
//    for (int i = 0; i <s.size() ; ++i) {
//        if(s[i]!= '.' || s[i]!=)
//    }
}