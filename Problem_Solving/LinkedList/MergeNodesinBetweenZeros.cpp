#include <string>
#include <map>
#include <iostream>
#include <vector>
#include <math.h>

using namespace std ;


struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(nullptr) {}
};

ListNode* mergeNodes(ListNode* head) {
    int comm =0 ;
    while (head->next){
        if(curr )
    }
}